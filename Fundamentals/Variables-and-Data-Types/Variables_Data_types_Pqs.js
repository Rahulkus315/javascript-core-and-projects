// ==============================================================
// Variables and Data types- Practice Questions
// ==============================================================
// 1. Declare a variable called 'name' and assign it your name as a string. Then, log the variable to the console.
let name = "Rahul kushwaha"; // string 
console.log(name);

// 2. Declare a variable called 'age' and assign it your age as a number. Then, log the variable to the console.
let age = 24;  // number
console.log(age);

// 3. Declare a constant variable called 'PI' and assign it the value of 3.14. Then, log the variable to the console.
const PI = 3.14;
console.log(PI);

// 4. Declare a variable called 'isStudent' and assign it a boolean value indicating whether you are a student or not. Then, log the variable to the console.
let isStudent = true;
console.log(isStudent);

// 5. Declare a variable called 'hobbies' and assign it an array of your hobbies. Then, log the variable to the console.
let hobbies = ["coding", "traveling", "cooking"];
console.log(hobbies);

// 6. Declare a variable called 'person' and assign it an object with properties 'name', 'age', and 'isStudent'. Then, log the variable to the console.
let person = {
  name: "Rahul",
  age: 21,
  isStudent: true
};
console.log(person);

// ===================== End of Variables_Data_types_Pqs.js ====================== //